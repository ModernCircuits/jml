#pragma once

namespace mc {
struct CommandIDs {
    enum Values {
        unknown,
        open,
        reload,
        save,
        saveAs,
        undo,
        redo,
    };
};
} // namespace mc
